### 1. Maximum finden (ohne `max`)
Gegeben ist eine Liste von Zahlen.

- Finde das **größte Element**
- Verwende **nicht** `max()`

### 2. Minimum finden (ohne `min`)
- Finde das **kleinste Element**
- Keine eingebauten Funktionen verwenden

### 3. Summe berechnen (ohne `sum`)
- Berechne die Summe aller Elemente einer Liste
- Nutze eine Schleife

### 4. Anzahl positiver Zahlen
- Gegeben ist eine Liste mit positiven und negativen Zahlen
- Zähle, wie viele Zahlen **größer als 0** sind

### 5. Durchschnitt berechnen
- Berechne den Durchschnitt einer Liste
- Nutze **keine** eingebauten Statistikfunktionen

### 6. Fakultät berechnen

Die Fakultät `n!` ist so definiert: `n! = 1 * 2 * 3 * ... * n`. Implementiere dies als Funktion:
```python
def fakultaet(n):
    ...
```

### 7. Fibonacci-Folge
- Gib die ersten `n` Fibonacci-Zahlen aus
https://de.wikipedia.org/wiki/Fibonacci-Folge

### 8. Teiler finden
- Lies eine Zahl `n` ein
- Gib alle Teiler von `n` aus

### ~~9. Primzahlen bis n~~
- Gib alle Primzahlen ≤ `n` aus (ggf. Lösung aus alter Aufgabe verwenden)

### ~~10. Primzahl-Test optimieren~~
- Prüfe nur bis √n

### ~~11. Duplikate entfernen (ohne `set`)~~
- Entferne doppelte Werte aus einer Liste

### 12. Häufigkeiten zählen
```python
daten = [1, 2, 2, 3, 3, 3, 4]
```

### 13. Zweitgrößte Zahl
- Sortieren ist nicht erlaubt

### 14. Liste umdrehen
- Ohne `reverse()` oder Slicing

### 15. Liste rotieren
```text
[1, 2, 3, 4] → [4, 1, 2, 3]
```

### 16. Zeichen zählen
- Zähle die Häufigkeit jedes Zeichens

### 17. Vokale zählen
- a, e, i, o, u (Groß/Klein beachten)

### 18. Wort umdrehen
- Ohne `[::-1]`

### 19. Anagramm prüfen
- z. B. „listen“ und „silent“

### 20. Längstes Wort
- Finde das längste Wort in einem Satz

### 21. Ziffern ausgeben
- Gib jede Ziffer einer Zahl einzeln aus

### 22. Dezimal → Binär
- Ohne `bin()`

### 23. Binär → Dezimal
- Nutze Schleifen

### 24. Palindrom-Zahl
- z. B. 12321

### 25. Perfekte Zahl
- Summe der echten Teiler = Zahl

### 26. Lineare Suche
- Gib Index oder -1 zurück

### ~~27. Bubble Sort~~
- Zähle Vergleiche

### ~~28. Selection Sort~~
- Erkläre den Unterschied

### 29. Duplikate finden
- Gib nur doppelte Werte aus
